export function* loginMethod(payload) {
    // try {
    //   let data = yield call(addHouseMethod, payload);
    //   yield put({ type: actionTypes.SAGA_ADDHOUSE, status: data });
    // } catch (err) {
    //   yield put({ type: actionTypes.ERROR });
    // }
}